<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <style name="AppDefault" parent="AppTheme">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <item name="colorOnSecondary">@color/onSecondary</item>
        <item name="colorOnBackground">@color/onBackground</item>
        <item name="colorOnSurface">@color/onSurface</item>
        <item name="colorOnError">@color/onError</item>
    </style>

    <style name="DialogDefault" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="colorPrimary">@color/dialogPrimary</item>
        <item name="colorSecondary">@color/secondary</item>
    </style>

    <style name="ButtonBorderless" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/onBackground</item>
    </style>

    <style name="TextView">
        <item name="android:textColor">@color/onBackground</item>
    </style>

    <style name="MaterialButtonBorderless" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/onBackground</item>
    </style>

</resources>
